TODO for v1.0:

* Change solver programs to accept n by multiplying MTtot and n

* Clear axes when switching between graphing vs. free and vs. total

* Write model for 2 MAPs bind MT-bound MAP

Bug fixes:

* X-axis selection box doesn't come back when changing from competition mode back to other modes